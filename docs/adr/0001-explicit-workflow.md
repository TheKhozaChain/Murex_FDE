# ADR 0001: Use an explicit controlled workflow

**Status:** Accepted

The investigation is implemented as visible stages—validate, gather, retrieve, synthesise, approve, record—instead of an unconstrained agent loop. This makes permissions, failure states, evidence lineage, observability, cost limits, and tests understandable to operators and reviewers.

